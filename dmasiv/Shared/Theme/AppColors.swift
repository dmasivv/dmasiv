import SwiftUI

struct AppColors {
    static let primary    = Color("Primary")       // main brand color
    static let secondary  = Color("Secondary")     // supporting color
    static let accent     = Color("Accent")        // highlights / CTAs
    static let background = Color("Background")    // page backgrounds
    static let surface    = Color("Surface")       // cards / containers
}
