import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            Text("User settings, e.g., default volume, lyric size")
                .navigationTitle("Settings")
        }
    }
}
